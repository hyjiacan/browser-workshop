
firefox 在 ftp 目录提供 sha256 数据，是否可以在下载后拿到这些数据执行校验呢？(获取缓存时，同时获取 sha256 数据)

配置 文件 ini ，内置分为 bws-serve.ini 和 bws-client.ini。 ini 内的注释 只能写在配置 行上方，不允许写在后面。
作为客户端时 和 serve 的 ini 必须放在与 二进制文件同一目录下，不再放到数据目录

serve 的 html 不要自动刷新 status。



制定 e2e 测试计划，要注意覆盖此程序在 serve 模式时，客户端与其交互的相关项，并完成测试